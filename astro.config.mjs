// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://brendoncabral.com', // Update this to your live domain
  base: '/',                         // Set this to the root
});
